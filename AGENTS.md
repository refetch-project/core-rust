# Refetch Core 工作规则

本文件适用于整个 `core-rust` 仓库。开始修改前必须完整阅读
[`docs/OPERATIONS.md`](docs/OPERATIONS.md)。

## 必须遵守

1. Concept 是语言无关的规范源，Rust 只实现当前锁定快照。不得为了让实现或测试通过而修改锁、快照、fixture、expected output 或排序语义。
2. 每个任务开始前记录起始 HEAD、分支、工作树状态、规范来源、假设、允许修改路径和明确非目标。发现工作树已有修改时，先区分所有权；不得覆盖、暂存、丢弃或顺带整理。
3. “继续”“完善”“优化”“顺便”只延续已经明确授权的范围，不授权切换规范版本、增加仓库、修改远端、提交、删除文件或扩张产品功能。
4. 目标、范围和允许的本地动作明确后，应在该范围内连续完成实现、测试、修复和收口，不得把每个文件、阶段或本地提交再次交给用户审核。结束时统一报告做了什么、发现的缺陷和下一步。
5. 以下动作分别需要用户明确授权，不能相互推导：修改其他仓库、升级 `SPEC_LOCK.json`、同步 `tests/spec/**`、提交、推送、创建 PR、合并、打 tag、发布、删除或移动文件、stash、清理工作树。
6. 禁止使用 `git reset --hard`、`git clean`、隐式丢弃本地修改或以删除方式解释“不要使用”。脏工作树无法安全隔离时停止；可以在得到本轮修改授权后使用独立 worktree 保留原状态。
7. Conformance fixture 必须真实发现、排序、逐个执行和计数。不得用 `if let Ok`、`.ok()`、`filter_map`、`unwrap_or_default` 或 catch-all-and-continue 吞掉失败。
8. 只有最终一次内容修改之后产生的验收结果才能支持完成声明。报告真实命令、结果、fixture 发现/执行数、Rust 测试数、完整 diff stat、发现的缺陷和下一步。
9. 严格区分“已检查”“已实现”“本地验证通过”“待人工审查”“维护者已确认”“已提交”“已推送”“已合并”“产品验证完成”。这些状态用于准确报告证据，不应被当作同一授权范围内逐阶段停工等待的理由。代理检查不能写成人工审查，单元测试不能写成产品接入完成。
10. 文档声称某个约束“已校验”“会拒绝”或“可重放”时，必须有对应的正向与负向自动化测试，或明确标注为尚未验证的要求。
11. Core 保持确定性、离线、来源无关和模型无关。Adapter、Analyzer、PiliNara/Flutter Host、网络、AI 和产品实验不得进入 Core，除非任务明确改变边界。

遇到规范空缺、来源冲突、数量不符、无法解释的错误或超出授权的必要动作时，停止修改并报告，不得自行补造隐藏语义。
